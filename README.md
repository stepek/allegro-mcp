# allegro-mcp
MCP server for Allegro REST API — tools generated at runtime from the official OpenAPI 3.0 schema. OAuth2 (device/code+PKCE/client_credentials) + compliant User-Agent on every request. Rust.
